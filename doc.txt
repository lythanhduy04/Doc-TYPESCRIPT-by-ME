# Doc-TYPESCRIPT-by-ME

đây là tài liệu tôi tự tìm hiểu và tích góp được

lệnh hay dùng

cấu hình typescript

file.ts || file.tsx

//install
npm install -g typescript

//check version
tsc -v

// convert from .ts to .js
tsc fileName

//set up tsconfig
install tsconfig
tsc --init
edit file: ctr + f
rootDir ./src
outDir ./dist

//auto run
tsc --watch
node --watch list/index.js

// dùng các gói (thư viện)
npm init -Y
npm install -D @types/node
